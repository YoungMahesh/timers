```bash
# .env variables are encrypted using @dotenvx/dotenvx package
# if you have .env.keys file with value of `DOTENV_PRIVATE_KEY`, you can decrypt .env file using:
bunx dotenvx decrypt -f .env
```
